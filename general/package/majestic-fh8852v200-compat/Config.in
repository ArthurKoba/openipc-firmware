config BR2_PACKAGE_MAJESTIC_FH8852V200_COMPAT
	bool "FH8626V100 Majestic FH8852V200 compatibility runtime"
	depends on BR2_arm
	depends on BR2_TOOLCHAIN_USES_MUSL
	select BR2_PACKAGE_JSON_C
	select BR2_PACKAGE_LIBEVENT_OPENIPC
	select BR2_PACKAGE_LIBOGG_OPENIPC
	select BR2_PACKAGE_LIBYAML
	select BR2_PACKAGE_MBEDTLS_OPENIPC
	select BR2_PACKAGE_OPUS_OPENIPC
	select BR2_PACKAGE_OPUS_OPENIPC_FIXED_POINT
	select BR2_PACKAGE_MAJESTIC_WEBUI
	help
	  Experimental FH8626V100 runtime using the FH8852V200 Majestic binary
	  and a staged source compatibility boundary.

	  Majestic HTTP/WebUI is untouched and continues to own its normal web
	  port and control plane directly. This package does not patch frontend
	  JavaScript, proxy Majestic HTTP, or replace its WebUI/API behavior.

	  No FH8852 kernel modules, firmware, load scripts or donor sensor
	  plug-ins are installed.

	  Explicit media bring-up uses source-built FH8626 adapters for:
	  - FH8852-shaped GC1054 sensor ABI;
	  - recovered SYS/VPSS/VENC operations;
	  - FH8626 VMM allocation;
	  - encoded-stream lease/release;
	  - recovered RTX audio transport.

	  The native-video path is deliberately constrained to the recovered
	  1280x720@25 H.264 Baseline contract. Unknown layouts remain explicit
	  staging boundaries rather than guessed compatibility.

	  This is compatibility staging, not production-ready Majestic support.
